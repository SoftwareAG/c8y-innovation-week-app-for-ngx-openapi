import { BsModalRef } from 'ngx-bootstrap/modal';
import { DroppedFile, GainsightService } from '@c8y/ngx-components';
import { ITrustedCertificate, TrustedCertificateService } from '@c8y/client';
import { AlertService } from '@c8y/ngx-components';
import * as i0 from "@angular/core";
export declare class AddTrustedCertificateComponent {
    private modal;
    private trustedCertificateService;
    private alertService;
    private gainsightService;
    maxAllowedFiles: number;
    AUTO_REGISTRATION_POPOVER: "All devices with credentials signed by this certificate will be able to communicate with the platform without a prior registration.";
    trustedCertificate: Partial<ITrustedCertificate>;
    fileIsUploaded: boolean;
    result: Promise<any>;
    private _save;
    private _cancel;
    PRODUCT_EXPERIENCE: {
        EVENT: string;
        CERTIFICATES: {
            COMPONENTS: {
                TRUSTED_CERTIFICATE: string;
            };
            ACTIONS: {
                ADD: string;
                UPDATE: string;
                DELETE: string;
            };
            RESULTS: {
                ADD_SUCCESS: string;
                UPDATE_SUCCESS: string;
                DELETE_SUCCESS: string;
            };
        };
        VERIFICATION_CODE: {
            COMPONENTS: {
                REGENERATE_CODE: string;
                VERIFY_CODE: string;
                DOWNLOAD_CODE: string;
            };
            ACTIONS: {
                REGENERATE: string;
                VERIFY: string;
                DOWNLOAD: string;
            };
            RESULTS: {
                REGENERATE_SUCCESS: string;
                VERIFY_SUCCESS: string;
                VERIFY_FAILED: string;
            };
        };
    };
    constructor(modal: BsModalRef, trustedCertificateService: TrustedCertificateService, alertService: AlertService, gainsightService: GainsightService);
    uploadFile(dropped: DroppedFile[]): void;
    save(): Promise<void>;
    close(): void;
    static ɵfac: i0.ɵɵFactoryDeclaration<AddTrustedCertificateComponent, never>;
    static ɵcmp: i0.ɵɵComponentDeclaration<AddTrustedCertificateComponent, "c8y-add-trusted-certificate", never, {}, {}, never, never, false, never>;
}
